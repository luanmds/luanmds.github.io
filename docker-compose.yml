services:
  hugo:
    image: hugomods/hugo:exts
    volumes:
      - .:/src
    working_dir: /src
    ports:
      - "1313:1313"
    command: hugo server --bind 0.0.0.0 --baseURL http://localhost:1313/ --buildDrafts --disableFastRender
    environment:
      - HUGO_ENV=development
